# Dictionary with the clean names for each method
METHOD_DICTIONARY = {
        "low_risk_q1": r'\texttt{LowRisk}($\gamma$)',
        "top_ranker_q1": r'\texttt{TopRanker}($\gamma$)',
        "random": r'\texttt{Random}($\gamma$)',
        "likes": r'\texttt{Likes}($\gamma$)',
        "tag": r'\texttt{Tag}',
        "None": r'\texttt{None}',
    }

HUE_ORDER = [r'\texttt{LowRisk}($\gamma$)', r'\texttt{Likes}($\gamma$)', r'\texttt{TopRanker}($\gamma$)', r'\texttt{Random}($\gamma$)',
                     r'\texttt{Tag}($g=39$)',
                     r'\texttt{Tag}($g=34$)',
                     r'\texttt{Tag}($g=67$)']

HUE_ORDER_SHORT = [r'\texttt{LowRisk}($\gamma$)',
                     r'\texttt{Tag}($g=39$)',
                     r'\texttt{Tag}($g=34$)',
                     r'\texttt{Tag}($g=67$)']

HUE_ORDER_ONLY_SAMPLING = [r'\texttt{Likes}($\gamma$)', r'\texttt{TopRanker}($\gamma$)', r'\texttt{Random}($\gamma$)']

